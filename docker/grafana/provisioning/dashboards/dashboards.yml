apiVersion: 1

providers:
  - name: 'Server Dashboards'
    orgId: 1
    folder: 'Server'
    folderUid: 'server'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
